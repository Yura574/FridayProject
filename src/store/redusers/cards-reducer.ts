
enum CardsAction {

}

type InitialState = {

}

const initialState: InitialState = {

}

type CardsActionTypes = any;

export const cardsReducer = (state: InitialState = initialState, action: CardsActionTypes) => {
    switch (action.type) {
        default:
            return state;
    }
}